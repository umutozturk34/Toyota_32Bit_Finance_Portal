package com.finance.app.analytics.dto;

/** Coarse risk band derived from a series' annualized volatility; the frontend localizes the label. */
public enum RiskLevel {
    LOW,
    MEDIUM,
    HIGH
}
